- Tests for server. See
  https://medium.com/@3n0ugh/how-to-test-grpc-servers-in-go-ba90fe365a18

- Bazel BUILD

- Optmize retrieval: map[key][]string so that words are in equivalence
  classes by key and we only iterate over keys. There's an even furthe
  optimization of sorting the keys and then doing a binary search for
  the start of the key space. (Only words with a key <= search key are
  candidates.)

- K8s pod spec

- Publish to a container registry

- Telemetry
